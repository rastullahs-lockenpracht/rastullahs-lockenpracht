IF (ENABLE_FMOD4)
	FIND_PACKAGE(FMODEx 4.20)
	IF (FMODEX_FOUND)
		ADD_SUBDIRECTORY(fmod4driver)
	ENDIF (FMODEX_FOUND)
ENDIF (ENABLE_FMOD4)

IF (ENABLE_OPENAL)
	FIND_PACKAGE(OpenAL)
	IF (OPENAL_FOUND)
		ADD_SUBDIRECTORY(openaldriver)
	ENDIF (OPENAL_FOUND)
ENDIF (ENABLE_OPENAL)